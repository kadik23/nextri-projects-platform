export const sendMagicLink = ({ email }: { email: string }) => {
  // see if the email is valide
  // see if the email exists in the database if so then get the esistsing user
  // if no then create nre user
  // send an email that has a loging token to the email
};
